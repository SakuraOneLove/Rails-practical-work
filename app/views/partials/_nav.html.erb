<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">

    <div class="navbar-nav">

      <a class="nav-item nav-link active" href="<%= grad_newsx_path %>"><%= t('main') %></a>

      <% if signed_in? %>

        <a class="nav-item nav-link active" href="<%= grad_main_path %>"><%= t('all_recipe') %></a>
        <a class="nav-item nav-link active" href="<%= grad_input_path %>"><%= t('add_recipe') %></a>
        <a class="nav-item nav-link active" href="<%= "http://localhost:3000/users/#{User.find_by(email: current_user.email).id}" %>"><%= current_user.email %></a>
        <a class="nav-item nav-link active" href="<%= session_logout_path %>"><%= t('exit') %></a>


      <% else %>

        <a class="nav-item nav-link active" href="<%= session_login_path %>"><%= t('entry') %></a>

      <% end %>

    </div>

  </div>

</nav>